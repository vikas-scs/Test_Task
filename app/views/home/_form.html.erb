<h1>Add URL</h1>
  <%= form_with(model: @project) do |form| %>
  <div>
    <%= form.label :enter_URL %><br>
    <%= form.text_field :github_url %>
</div>
<div class="actions">
    <%= form.submit "clone" %>
 </div>
 <% end %>